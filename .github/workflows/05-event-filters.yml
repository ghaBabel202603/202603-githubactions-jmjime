name: 05 - Event Filters
on:
  push:
    branches:
      - main
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: "Mostrar SO"
        run: |
          echo "Este trabajo se ejecuta en un Runner de Ubuntu"
          echo $RUNNER_OS